{
  "name": "teampulse",
  "version": "1.0.0",
  "description": "Collaborative workspace for student-mentor project management",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed"
  },
  "keywords": ["mern", "collaboration", "project-management", "real-time"],
  "author": "TeamPulse",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
